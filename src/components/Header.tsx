import { Typography } from "@mui/material";

export default function Header() {
    return (
        <header className="ghost-header">
            <Typography className="ghost-title" variant="h3">ghost</Typography>
        </header>
    );
}